# SUPERSEDED — 2026-08-16 — DO NOT ACT ON THIS FILE

The premise of this handoff was WRONG. There is no lossy chain and nothing to repair.

The 153 "missing" directives were REMOVED ON PURPOSE by the approved v5 directives migration
(apply-directives-v5-canonical.mjs, stamped appliedOn 2026-08-16). They are the blessings,
quoted Scripture, prayer text, third-party commands and misfiled claims/questions/predictions
identified by the directives adjudication. 2,705 adjudicated - 153 removed = 2,552 kept.

THE CERTIFIED DIRECTIVE TOTAL IS 2,552. Do NOT restore the 153. Do NOT "repair" the migration.

How the false alarm happened: local was compared against PRODUCTION, which is seed 71 and
predates the migration. A difference from production is not a loss. Two probe errors compounded
it - reading f.v5Migration instead of f.totals.v5Migration (concluding the run-once guard had
failed), and treating git HEAD 2,422 as drift when it is simply an older commit. The shrinking
source-boundary debt was the expected consequence of removing 153 over-extending directives,
not corroborating evidence of loss.

apply-entities MERGES postAnalysis (p.postAnalysis.namedEntities = ...) and does not replace it.
Prime suspect cleared by code and by test.

Everything below is retained only for the STAGED WORK inventory, which remains accurate.

---

# HANDOFF — lossy chain repair, then combined seed-72 preview

Production is on **seed 71** and is correct. Do not deploy. Do not run chain steps until the
loss below is repaired.

## The loss

    actionRequests         2,705  ->  2,552   (-153)   apply-directives.mjs consumes this
      derived from
    directives-final.json  2,422  ->  2,276   (-146)   regenerated by an earlier chain step
    questions.json         also modified (currently 6,577 total, 134 editorialNormalization)
    post text              1,128,312 chars — UNCHANGED, so nothing was re-parsed

`apply-directives.mjs` is **not** the bug. It faithfully consumes a shrunken
`audit/directives-final.json`. Isolated by running it alone against a restored posts.json:

    restored from live      2705
    apply-entities          2705   <- innocent
    apply-directives        2552   <- loss appears here
    full chain replay       2552

## Prime suspect

`apply-entities.mjs` rewrites `postAnalysis` on every post at the end of its run. If that write
REPLACES the postAnalysis object instead of merging only entity-owned fields, every sibling field
(questions, directives, themes, claims, predictions) is thinned, and each downstream derive step
then re-derives from a poorer post. That would explain a cascade rather than one isolated count.
Check this first — it is a single-line question in the materialiser.

## Corrections to earlier assumptions (I got these wrong)

- **Git HEAD is NOT seed 71.** Last commit is "Certify Codes & Brackets"; its
  `directives-final.json` holds 2,422 rows, which is neither the certified 2,705-era state nor the
  current 2,276. Do not restore from git.
- **Restore the COMPLETE certified artifact set from production**, not just `posts.json`. I
  restored only posts.json from https://qdrops.app/data/posts.json, which is insufficient and does
  not survive a replay.
- **Do not paste the 146 missing rows back** into the shrunken file. Regenerate
  `directives-final.json` from the certified baseline once the upstream step is fixed.

## Ownership rule to enforce with tests

The entity step may modify `namedEntities` and explicitly entity-owned metadata ONLY. Questions,
directives, themes, claims, predictions and every unrelated field must survive
occurrence-for-occurrence — not merely count-for-count.

## Chain must prove, after repair

    raw actionRequests            2,705
    questions                     6,577 AND the same occurrence multiset, not just the count
    post text                     1,128,312 characters
    entity rendered count         == certified entity count exactly
    no unexplained loss in any certified category

## Staged and complete — do not redo

**679-row Reference batch** (fully applied locally, verified):

    postAnalysis.namedEntities 9,750  ==  certified mentions 9,750   difference 0
    queue 107 — Reference 31, Subject 16, Notation 29, Device 31
    all 31 held Reference rows open AND carrying their audit notes
    679 audit rows, unique, zero unmatched, zero duplicated
    173 occurrence-scoped rulings covering 552 occurrences; 89 canonicals created
    5 partial-token rows (AL inside AL-Q, MS inside MS-13) closed WITHOUT highlights
    2 near-duplicate canonicals merged; 0 type conflicts

Fixes made this session that must be preserved:

- `apply-entities.mjs` — a recount now RETRACTS the superseded ruling's render entries, keyed by
  canonical+alias. Without it postAnalysis carried 62 more entries than certified (BO -> Barack
  Obama and PS -> Peter Strzok, the only alias+canonical pairs ruled in two batches).
- `build-resolution-queue.mjs` — clearing is occurrence-scoped like counting, and scopes are
  collected per alias as a LIST. Post-level clearing had erased the held row at #1385 line 5
  ("SIS is good?"). A Map keyed by alias kept only the last ruling.
- 7 rulings extended to re-include 26 context-resolved posts (scoped-recount hazard).
- 5 regression invariants added — audit-cross-section is now 146, not 141.
- `CANONICAL.entities` in contracts.mjs and the sectionInfo headline figures updated.

## Then, in ONE combined candidate

1. Reapply the 679-row Reference batch.
2. Apply the approved Post #2774 ruling: `DELTA-2774-1-16` -> RESOLVED_NON_ENTITY, gloss
   "zero-minute or same-displayed-time Q/POTUS posting interval". `Q/POTUS` yields TWO spans and
   is never merged. Coordination is the post's CLAIMED implication, not a verified fact.
3. Body-`Q` entity rule: body self-references resolve to the Q persona; the TERMINAL standalone
   `Q` is a signature, excluded structurally (last non-empty body line, evaluated apart from
   quoted blocks) — never post-level suppression, because #2774 and #2775 contain both.
   The 236 nonterminal candidates across 180 posts get a REVIEW pass, not a regex sweep.
   Exclude 10-Q, AL-Q, Q&A, "Q:", ?q=, Q Clearance.
   Leave untouched: #1176 Delta Air Lines, #756/#757/#785 "Q, DELTA" sign-offs, #804
   "DELTA [6] CONF.", #2876 PRO POTUS/Q.
4. Regression checks — positive: #2774, #2775, #2567, #365, #2876. Negative: #2588 (10-Q),
   #1887 (AL-Q), #1192 (Q&A), #48 (Q Clearance), every terminal signature.

Expected queue after #2774: **106 total — Reference 30, Subject 16, Notation 29, Device 31**.

## Deployment

Private preview only. Report the preview URL and a complete before/after manifest. Production
stays on seed 71 until the owner tests and explicitly approves. Do NOT rebuild between preview
approval and production promotion — promote the identical artifact.
